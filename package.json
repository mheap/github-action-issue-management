{
  "name": "github-action-issue-management",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "start": "node ./index.js",
    "test": "jest",
    "build": "npx @vercel/ncc build && npx convert-action"
  },
  "dependencies": {
    "@mheap/action-router": "^1.2.0",
    "actions-toolkit": "^4.0.0"
  },
  "devDependencies": {
    "jest": "^25.2.7"
  }
}
